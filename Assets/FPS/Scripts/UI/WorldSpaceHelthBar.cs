using UnityEngine;
using UnityEngine.UI;
using Unity.FPS.Game;

namespace Unity.FPS.UI
{
    /// <summary>
    /// 캐릭터의 머리위에 있는 HeathBar UI 관리
    /// 1. 현재 heath값에 따른 게이지 관리
    /// 2. 월드 캔바스 UI - 게이지바가 항상 플레이어(카메라)를 바라본다
    /// 3. 현재 heath값이 maxHealth이면 게이지바 UI를 보이지 않게 한다
    /// </summary>
    public class WorldSpaceHelthBar : MonoBehaviour
    {

    }
}